apply plugin: 'java'

repositories {
  mavenCentral()
}

dependencies {
  compile 'org.slf4j:slf4j-api:1.7.2'
}

task generateAst(type:Exec) {
  commandLine 'python2', 'convert_protofile.py'
}

jar {
  dependsOn generateAst
  archiveName = "rethinkdb.jar"
}