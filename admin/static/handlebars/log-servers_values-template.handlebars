
    Set table <a href="#tables/{{namespace_id}}">{{namespace_name}}</a>'s {{attribute}} to:
    <ul class="pretty_logs_ul">
        {{#each servers}}
            <li class="pretty_logs_li">Server <a href="#servers/{{server_id}}">{{server_name}}: {{value}}</li>
        {{/each}}
    </ul>


