#!/bin/sh

# My apologies for having this build script instead of putting this in the build
# system.  Run it with ./scripts/buildtableview.  You have to run it from the
# root directory of the repo.

# This is known to work with typescript version 3.6.2
tsc --strict -t ES2016 ./admin/static/js/tableview.ts
