desc: 715 -- noreply writes
table_variable_name: tbl
tests:

  - cd: tbl.insert({})
    ot: partial({'inserted':1})

  - cd: tbl.insert({})
    runopts:
      noreply: false
    ot: partial({'inserted':1})

  - cd: tbl.insert({})
    runopts:
      noreply: true
    testopts:
      noreply_wait: true
    ot: null

  - cd: tbl.count()
    ot: 3

