desc: Tests of conversion to and from the RQL null type
tests:
    - py:
        cd: r.expr(None)
        ot: None
      js:
        cd: [r(null), r.expr(null)]
        ot: "null"
      rb:
        cd: r nil
        ot: nil

    - py: r.expr(None).type_of()
      js: r.expr(null).type_of()
      rb: r(nil).type_of()
      ot: "'NULL'"

    # What other behavior do we need to test here?

    # expected error cases
    # ...
