#!/usr/bin/env python

import os, sys, subprocess

# -- run test

command = [os.environ.get('INTERPRETER_PATH', 'python3.5'), os.path.join(os.path.dirname(__file__), 'asyncio_connection_3_5.py')]
sys.exit(subprocess.call(command, stderr=subprocess.STDOUT))
